test "banner item":
	set {_pattern} to a creeper banner pattern item
	assert {_pattern} is set with "Creeper banner pattern should return an item"

	parse:
		set {_invalid} to a base banner pattern item
	assert last parse logs contains "There is no item for the banner pattern type 'BASE'." with "Base banner pattern type should not get an item (unless updated)"
